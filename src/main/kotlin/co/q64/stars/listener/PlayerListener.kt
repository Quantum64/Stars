package co.q64.stars.listener


object PlayerListener {
    /*
    @SubscribeEvent
    fun onCapabilityAttachWorld(event: AttachCapabilitiesEvent<World>) {
        if (event.getObject().getDimension() is HubDimension) {
            event.addCapability("hub".id, hubCapabilityProvider.get())
        }
    }
     */
}